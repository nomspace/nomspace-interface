import styled from "@emotion/styled";
import { Box } from "theme-ui";

export const Card = styled(Box)({
  borderRadius: "20px",
  padding: "32px",
  backgroundColor: "rgba(13, 19, 27, 0.7)",
  boxShadow: "rgb(0 0 0 / 5%) 0px 16px 32px 0px",
});
